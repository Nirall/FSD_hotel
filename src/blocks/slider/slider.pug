mixin slider
  -
    var text = "диапазон цены"
    if (attributes.type === "for ui-kit")
      text = "range slider"
  .slider.js-slider
    .slider__title
      h3.slider__title-text= text
      .slider__price.js-slider__price= ""
    .slider__body.js-slider__body