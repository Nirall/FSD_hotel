mixin checkbox-item(text)
    -
        var classMod = "checkbox-item"
        if (attributes.style == "bold") {
            classMod += " checkbox-item_bold"
        }
    div(class = classMod)
        label
            input(type = "checkbox")
            span.checkbox-item__pseudo
            span!= text